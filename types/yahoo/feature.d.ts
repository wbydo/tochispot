import { InfoWindow, InfoOptions } from "./infoWindow";

export declare class Feature {
  public bindInfoWindow: (content: string | Node, infoWindowOptions?: InfoOptions) => InfoWindow;
  constructor()
}
